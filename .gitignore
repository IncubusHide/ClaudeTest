# Dependencies
node_modules/

# Build output. Note `/build/` is anchored to the repository root on purpose:
# apps/wardrobe/build holds electron-builder's resources (the app icon and the
# scripts that draw it), which are source and must be committed.
dist/
out/
/build/
*.tsbuildinfo

# Electron packaging
release/

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Test coverage
coverage/
